# Copier vers .env et remplir — le fichier .env n'est PAS versionné (secrets).
# Partagé par les deux backends pendant la transition (Django lit ce même fichier).
APP_NAME=ERP KILIMA HOLDINGS
ENVIRONMENT=dev
DATABASE_URL=sqlite:///kilima_dev.db
SECRET_KEY=remplacer-par-une-cle-secrete-longue-et-aleatoire
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=720
